user: ubuntu
root_dir: /opt/salt_root
verify_env: True

file_roots:
  base:
    - /opt/salt_root/srv/salt

pillar_roots:
  base:
    - /opt/salt_root/srv/pillar

# Disable master config from showing in pillar
pillar_opts: False

peer:
  .*:
      - .*

# Where our external pillar and other custom modules live
extension_modules: /opt/salt_root/srv/extmods

# Enable our custom external pillar that provides additional
# pillar data specific to a particular stack
ext_pillar:
  - stack_pillar: ''
